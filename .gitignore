node_modules/
dist/
.safehubby/
*.log
.DS_Store

# Capacitor native projects are COMMITTED: the manifest, permissions, signing
# config and ProGuard rules are real source, and regenerating them on every
# clone would lose all of it. Only the build output and local machine paths
# are ignored.
apps/web/android/.gradle/
apps/web/android/build/
apps/web/android/app/build/
apps/web/android/local.properties
apps/web/android/app/src/main/assets/public/
apps/web/ios/App/App/public/
apps/web/ios/App/Pods/
apps/web/ios/App/build/

# Signing material — never commit these.
keystore.properties
*.jks
*.keystore
